**Fix PDF Generation Error in KDP Cover Creator**

When generating the **Book Cover PDF** in the **KDP Cover Creator** section, the application throws the following runtime error:

> **PDF Generation Failed**
> `calculateSpineWidth is not defined`

This error prevents the PDF from being generated successfully.

### Required Fix

1. Identify why `calculateSpineWidth` is undefined.

   * Ensure the function exists.
   * Ensure it is correctly imported wherever it is used.
   * If the function was renamed or moved, update all references accordingly.
   * If it no longer exists, recreate it and use a single shared implementation throughout the application.

2. Before PDF generation starts, validate that all required helper functions are available, including:

   * `calculateSpineWidth`
   * KDP size calculations
   * Bleed calculations
   * Safe area calculations
   * Margin calculations
   * Spine positioning calculations
   * Barcode safe area calculations

3. Add proper error handling.

   * Never allow an undefined function to crash PDF generation.
   * Display a meaningful error only if a required calculation genuinely fails.
   * Log the exact source of the error for debugging instead of showing a generic failure.

4. Ensure the same spine width calculation is used consistently across:

   * Live Cover Preview
   * Spine Preview
   * Paperback Cover PDF
   * Hardcover Cover PDF
   * Final Print PDF

5. Verify that spine width is recalculated automatically whenever any of the following changes:

   * Interior page count
   * Paper type
   * Print type
   * Paperback/Hardcover selection
   * Trim size
   * Bleed option

6. After fixing the error, perform a full validation by generating cover PDFs for multiple page counts (for example, 24, 100, 200, and 500 pages) to confirm that:

   * No `calculateSpineWidth` errors occur.
   * The spine width is calculated correctly.
   * The preview matches the exported PDF exactly.
   * All KDP cover dimensions, bleed, safe areas, margins, spine, and barcode area are rendered correctly.

### Required Fix: INDEX Heading & Alphabetical Section Layout

As shown in the attached screenshot, the **INDEX** page has two layout issues:

1. The **alphabetical headings (A, B, C, D, etc.)** are not positioned consistently.
2. The index entries are overflowing into the **INDEX** title area, causing text overlap.

Implement the following layout logic:

* Center every alphabetical heading (**A, B, C, D... Z**) within its allocated column or section.
* Use a consistent font size, weight, alignment, and spacing for all alphabetical headings throughout the Index.
* Reserve a fixed header area at the top of every Index page exclusively for the **INDEX** title.
* Do not allow any index entries, page numbers, or alphabetical sections to render inside or overlap the reserved header area.
* Begin rendering index entries only below the defined content start position, leaving sufficient vertical spacing beneath the **INDEX** heading.
* Before placing each alphabetical section, calculate the remaining printable space on the page. If there is insufficient space, automatically move the entire section to the next Index page.
* Maintain consistent top margins, side margins, borders, line spacing, and typography on every Index page.
* Validate the layout before exporting the PDF to ensure that no text overlaps the **INDEX** title, alphabetical headings, borders, or page footer.

### Expected Result

* The **INDEX** title remains clean, centered, and unobstructed.
* Every alphabetical heading (**A, B, C... Z**) is perfectly centered and consistently aligned.
* No index entries overflow into the title area.
* The Index pages remain clean, readable, professionally formatted, and fully compliant with Amazon KDP print standards.

### Existing Book 2 — Replace the Old 31 Grid Facts

The previously implemented uniqueness logic is correct for **future books**, but existing project **Book 2 already contains 31 old/reused Grid Content facts**. Fix Book 2 as well.

### Required Logic

* Locate the existing **31 Grid Content facts in Book 2**.
* Remove/replace those old Grid facts.
* Generate **31 completely new facts** that match the same Grid Content concept, purpose, and subject requirements.
* The new 31 facts must **not appear in Book 1, Book 2, or any other previously generated book**.
* Check every replacement against the **entire historical fact database**, not just the current book.
* Also check the 31 new facts against each other to ensure there are no duplicates or near-duplicates.
* Do not simply rewrite the old facts by changing a few words. Each replacement must represent a **genuinely different fact**.
* Preserve the existing **31-grid layout, image placement, design, dimensions, and styling**. Only replace the fact content/data.
* Once the new 31 facts are accepted, permanently mark them as **used** so they cannot appear in any future book.

### Book 2 Correction Flow

```text
Existing Book 2
      ↓
Find existing 31 Grid Facts
      ↓
Mark old/reused Grid Facts as invalid for Book 2
      ↓
Generate 31 genuinely new Grid Facts
      ↓
Compare against ALL previously used facts
      ↓
Compare against each other
      ↓
Duplicate / similar fact?
   YES → Reject → Generate replacement
   NO  → Accept
      ↓
Update Book 2 with the new 31 facts
      ↓
Save new facts to permanent used-fact history
      ↓
Future books cannot reuse them
```

### Important

**Do not regenerate the entire Book 2.** Only replace the **31 existing Grid Content facts** with 31 new, unique facts while keeping everything else in Book 2 unchanged.

The final result must be:

**Book 2 = same book/layout + 31 completely new Grid Content facts**, with permanent duplicate protection applied to all future books.

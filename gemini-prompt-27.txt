### Required Fix: SVG Images Are Rendering Incorrectly in the PDF

In the previous implementation, the application correctly created the required pages in the book, The page insertion logic is working correctly—the required pages are being created in the correct locations. However, the SVG transparent images are **not rendering properly**. Instead of displaying as clean vector graphics, they appear extremely dark, nearly invisible, or only show faint outlines, as shown in the attached screenshot.

Implement the following logic to fix this issue:

* Load and render the SVG files using a **true SVG renderer**. Do not treat SVG files as plain text, masks, fonts, or unsupported image objects.
* Preserve all original SVG properties, including fills, strokes, transparency, gradients (if any), viewBox, and clipping paths.
* Render the SVG exactly as it appears in the original file without changing its colors, opacity, or line thickness.
* Do not invert colors or apply automatic dark-mode, transparency, opacity, blend-mode, or color-conversion filters during rendering.
* Ensure the renderer correctly interprets all SVG elements (`<path>`, `<rect>`, `<circle>`, `<polygon>`, `<g>`, `<defs>`, etc.).
* Embed the SVG as high-quality vector artwork whenever the PDF library supports it. If vector embedding is not supported, rasterize the SVG at **300 DPI or higher** before embedding to maintain print quality.
* Verify that the SVG is fully rendered before exporting the PDF. If rendering fails, stop the export and report the error instead of generating an incorrect page.
* Preserve transparency so that only the artwork is visible and the page background remains clean.
* Automatically scale the artwork proportionally to fit within the printable area while maintaining the original aspect ratio. Never stretch, crop, or distort the image.
* Keep the existing page border, margins, and layout unchanged.
* Ensure the final PDF displays the SVG exactly like the original source file—crisp, sharp, high-resolution, and fully visible.

### Validation Before PDF Export

Before generating the final PDF, perform these checks:

1. Confirm that every SVG file has loaded successfully.
2. Confirm that each SVG has rendered correctly (not blank, black, transparent, or outline-only).
3. Confirm that every inserted page contains the expected artwork.
4. If any validation fails, stop PDF generation and display an error identifying the affected SVG instead of producing an incorrect PDF.

**Expected Result:** Every inserted SVG page should match the original SVG file exactly, with correct rendering, proper visibility, sharp vector quality, and professional Amazon KDP print-ready output.

previous your fixes not working first read given instructions carefully and use your brain fully and perfect logic to resolve this issue : ### Required Fix: MAP OF YOUR ADVENTURE SVG Upload Logic

On the **"MAP OF YOUR ADVENTURE"** page, the SVG icon upload system is not updating correctly. When the user uploads a new set of transparent SVG icons, the application continues to display previously uploaded icons instead of replacing them. Additionally, if the user leaves some upload slots empty, those empty slots incorrectly retain and display old images from previous uploads.

Implement the following logic:

* Treat each new upload as a completely new icon set.
* Before processing any new upload, **clear all previously stored SVG icons** associated with the "MAP OF YOUR ADVENTURE" page.
* Delete all old icon references, cached files, object URLs, and preview data before loading the new files.
* Replace the previous icon set entirely with the newly uploaded SVG files.
* If an upload slot is left empty, **do not reuse or display the previous icon** in that position. Empty slots must remain empty (or display the default placeholder if one exists).
* Never preserve old icons for upload slots that are not included in the latest upload.
* Refresh the preview immediately after uploading so that only the current SVG files are displayed.
* Ensure the generated PDF uses **only the latest uploaded SVG icons** and never references cached or previously uploaded files.
* Invalidate all browser, application, and memory caches related to the icon upload whenever a new upload occurs.
* Maintain a one-to-one mapping between upload slots and SVG files, ensuring each slot always displays the correct current icon.
* Apply this replacement logic consistently to the editor preview, PDF proof, final PDF generation, and all future exports.

### Expected Behavior

* **New upload → Completely remove old icons → Load only new icons.**
* **Empty upload slot → Empty output (or default placeholder), never an old cached icon.**
* **No previous SVG icon should ever appear unless it is part of the current upload set.**

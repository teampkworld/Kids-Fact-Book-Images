In the **"MAP OF YOUR ADVENTURE" → Bulk SVG Upload (Index)** section, the SVG transparent icons uploaded by the user are **not rendering correctly**. As shown in the attached screenshot, the icons are displayed as **solid black square boxes**, with the actual SVG artwork barely visible or completely hidden. This indicates that the SVG background, masking, fill, or rendering logic is incorrect.

Implement the following logic:

* Render uploaded SVG files exactly as transparent vector graphics. **Only the icon artwork should be black**; the surrounding background must remain fully transparent.
* Do **not** rasterize the SVGs. Preserve the original vector paths, viewBox, aspect ratio, and transparency.
* Remove or ignore any background rectangles (`<rect>`), masks, clipping paths, or filled shapes that create a solid black square behind the icon.
* Normalize every SVG so that:

  * All visible icon paths render as **solid black (#000000)**.
  * The SVG background remains **100% transparent**.
  * Only the icon silhouette is visible.
* Preserve the original icon geometry and proportions. Do not crop, stretch, distort, or scale disproportionately.
* Strip any embedded CSS, inline styles, or unsupported SVG attributes that interfere with PDF rendering.
* Correctly process all common SVG elements (`<path>`, `<rect>`, `<circle>`, `<ellipse>`, `<polygon>`, `<polyline>`, `<line>`, `<g>`, etc.).
* If an uploaded SVG contains a full-background rectangle or canvas shape, treat it as a background element and remove it during import rather than rendering it.
* Scale each icon proportionally to the required display size while keeping it centered within its allocated area.
* Render the icons using a high-quality SVG rendering engine so the exported PDF retains crisp vector quality suitable for Amazon KDP printing.
* Validate every uploaded SVG before PDF generation. If a file cannot be parsed correctly, display an import error instead of producing a corrupted icon.

### Expected Result

The **"MAP OF YOUR ADVENTURE"** page should display **clean, sharp, transparent SVG silhouette icons**, with **only the icon itself rendered in solid black**. No black square backgrounds, clipping artifacts, missing shapes, or rendering errors should appear. The icons should match the original uploaded SVG artwork while maintaining full vector quality in the exported PDF.

### Current Book 2 — Replace the Existing 31 Grid Facts

**Book 1 has already been created, and the project is currently working on Book 2.** The current Book 2 already contains **31 old Grid Content facts**, which must now be replaced.

Implement the following logic:

* Keep **Book 1 unchanged**.
* In the **current Book 2**, identify the existing 31 Grid Content facts.
* Replace all 31 old Grid facts with **31 completely new and unique facts** that follow the same **31 Grid Content concept**.
* The new facts must **not be reused from Book 1**.
* The new facts must also be unique from one another within Book 2.
* Do **not** merely rewrite or paraphrase the old facts. Generate genuinely different facts.
* Preserve the existing **31-grid structure, layout, image positions, styling, and formatting**. Only replace the fact content.
* After generating the replacements, permanently record the 31 new facts as used so they cannot be selected again for future books.

### Duplicate-Protection Logic

```text
Book 1
  ↓
31 Grid Facts already used
  ↓
Current Book 2
  ↓
Find existing 31 old Grid Facts
  ↓
Remove/replace old Grid Facts
  ↓
Generate 31 NEW Grid Facts
  ↓
Compare against Book 1 + all previously used Grid Facts
  ↓
Compare the 31 new facts against each other
  ↓
Duplicate or previously used?
   YES → Reject → Generate replacement
   NO  → Accept
  ↓
Save the 31 new facts to permanent used-history
  ↓
Update Book 2
```

### Critical Rule

The final result must be:

**Book 1 → remains unchanged**
**Book 2 → receives 31 completely new Grid Content facts**
**Future Books → cannot reuse any previously used Grid Content fact**

Apply this logic specifically to the **current Book 2** without regenerating or changing the rest of the book.

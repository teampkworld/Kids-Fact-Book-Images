Add Image Drag-and-Drop System and Control Illustration Text Limits

## 1. Add Drag-and-Drop Feature for All PDF Book Images

Implement a complete drag-and-drop image placement system inside the PDF book application.

Requirements:

* Every image used in the PDF book app must support drag-and-drop functionality.
* Users should be able to:

  * Select an image.
  * Drag the image.
  * Drop it directly into the assigned image placeholder area.
  * Automatically place the image into the correct location.

The code should ensure:

* Smooth drag interaction.
* Correct placeholder targeting.
* Image position accuracy.
* Proper image replacement when a new image is dropped.
* No broken placements or incorrect image assignments.

This feature should work consistently across:

* Current books.
* Future generated books.
* All image upload sections.
* All illustration placeholders.

---

# 2. "AMAZING FACT DISCOVERY — CUSTOM ILLUSTRATIONS UPLOADER" Text Control System

In the section:

**"AMAZING FACT DISCOVERY — CUSTOM ILLUSTRATIONS UPLOADER"**

Apply the following rules permanently for all current and future image prompts.

---

## Image Prompt Text Limit

For every generated image prompt:

The total number of text elements must not exceed **3 maximum**.

The three text elements include:

1. Educational text labels
2. Call-out labels
3. Explanatory text

Combined total:

**Maximum allowed = 3 text elements per image prompt**

Do not generate images containing excessive text, multiple labels, or crowded educational annotations.

---

# 3. Text Size and Visibility Improvement

Update the code so that all text elements inside generated illustrations appear in a larger font size.

Requirements:

* Increase text readability.
* Use larger educational labels.
* Ensure call-out text is clearly visible.
* Avoid tiny or unreadable text.
* Maintain clean professional educational design.

The generated illustrations should prioritize:

* Clear visuals.
* Limited text.
* Large readable typography.
* Child-friendly educational presentation.

---

# 4. Apply Rules Permanently

These settings must become default behavior for:

* Current image generation.
* Future image prompts.
* New books.
* New manuscript volumes.
* All custom illustration uploads.

The system must automatically enforce:

✅ Drag-and-drop images everywhere
✅ Maximum 3 text elements per illustration
✅ Larger readable text size
✅ Clean educational illustration layout

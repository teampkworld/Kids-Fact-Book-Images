## Update Table of Contents SVG Icons

### Required Implementation

On the **Table of Contents** page, add the correct SVG icons for the following headings:

* **MY FAVORITE FACTS**
* **FACT EXPLORER CHALLENGE**
* **AMAZING FACT COLLECTOR**

Each heading must display its corresponding SVG icon with the same styling, alignment, and quality as the other Table of Contents icons.

---

## Icon Size Standardization

The SVG icons for these headings currently appear **too small**, especially those with transparent backgrounds.

Update the rendering so that:

* Increase the size of the SVG icons to match the visual size of all other Table of Contents icons.
* Maintain the original aspect ratio without stretching or distortion.
* Center each icon correctly within its allocated space.
* Ensure consistent spacing between the icon and its heading.
* Transparent SVG icons should occupy the same visible area as the other icons and must not appear smaller.

---

## SVG Quality Requirements

All SVG icons must be rendered with professional quality.

Ensure every SVG is:

* Sharp and crisp.
* Clean and smooth.
* Bright and clearly visible.
* High-quality vector rendering.
* Perfectly aligned.
* Free from pixelation, blur, clipping, or distortion.
* Rendered consistently in both the application preview and the generated PDF.

---

## Automatic SVG Update Detection

Implement automatic asset synchronization for all SVG icons.

Whenever an SVG file is updated in the GitHub repository—even if the **filename and URL remain unchanged**—the application must:

* Detect that the SVG asset has changed.
* Refresh or invalidate any cached version.
* Download and use the latest SVG automatically.
* Replace the previous version without requiring manual code changes.

This automatic update mechanism must work for **all future SVG updates**, ensuring the application always uses the most recent assets.

---

## Validation Checklist

Before generating the final PDF, verify that:

* ✅ The correct SVG icon is displayed for **MY FAVORITE FACTS**.
* ✅ The correct SVG icon is displayed for **FACT EXPLORER CHALLENGE**.
* ✅ The correct SVG icon is displayed for **AMAZING FACT COLLECTOR**.
* ✅ All three icons match the size of the other Table of Contents icons.
* ✅ Transparent SVGs no longer appear visually smaller.
* ✅ Every SVG is sharp, clean, bright, and perfectly rendered.
* ✅ The latest GitHub SVG files are always used.
* ✅ The preview and generated PDF display the same updated icons.
* ✅ Future SVG updates are detected and applied automatically without manual intervention.

### Expected Result

The Table of Contents should display a consistent, professional set of SVG icons. The three specified headings must always use the latest SVG assets, rendered at the correct size and quality, with automatic detection and application of future GitHub SVG updates.

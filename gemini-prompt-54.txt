### Fix the "TABLE OF CONTENTS" Page Generation Logic

The **TABLE OF CONTENTS** page must always generate **identically on every computer**. The same project, data, and code must always produce the exact same Table of Contents page without any missing entries, blank areas, or layout inconsistencies. i attached two system geneated table of content pages screenshots for your clarification 

#### Required Logic

* Generate the **TABLE OF CONTENTS** only after the entire book structure and final page numbers have been completed.
* Before rendering the page, ensure that all required resources for the Table of Contents are fully loaded, including headings, page numbers, SVG icons, fonts, and layout data.
* Do not begin rendering until every required resource has successfully completed loading.
* Remove all race conditions and asynchronous timing issues that could cause incomplete rendering.
* Render every Table of Contents entry sequentially and ensure the rendering process cannot terminate early because of a single failed entry.
* If an individual entry or icon fails to load, log the error, skip only that item if necessary, and continue rendering all remaining entries.
* Ensure that every heading, SVG icon, leader dots, and page number is rendered consistently and aligned correctly.
* Use embedded fonts only. Do not rely on system-installed fonts.
* Use fixed PDF measurements for spacing, alignment, margins, icon size, and typography so the layout remains identical across all computers.
* Validate the completed Table of Contents before exporting the PDF. Verify that:

  * All expected entries are present.
  * All page numbers are finalized.
  * All SVG icons have loaded successfully.
  * No pending asynchronous operations remain.
  * No blank sections exist.
  * No text or icons are missing or overlapping.
* If validation fails, stop the PDF export, report the exact error, and regenerate the Table of Contents instead of exporting an incomplete page.

### Expected Result

The **TABLE OF CONTENTS** page must be fully deterministic. Regardless of the computer, operating system, browser, cache state, or internet speed, the generated page must always be complete, visually identical, and fully compliant with Amazon KDP print standards.

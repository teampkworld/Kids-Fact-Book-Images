Modify **all rounded pill/badge elements throughout the entire book** by changing them to **rectangular rounded badges** (rounded rectangle shape with consistent corner radius). Apply this change uniformly across every page where these badges appear.

### Requirements

* Replace every existing **fully rounded pill/badge** with a **rectangular rounded badge**.
* Maintain a consistent corner radius for all badges throughout the book.
* Preserve the existing badge height, padding, text alignment, icon alignment, and overall visual hierarchy.
* Ensure the badge automatically resizes horizontally to fit its content while maintaining consistent vertical dimensions.
* Keep the existing colors, fonts, font sizes, font weights, icons, and text exactly the same unless specified elsewhere.
* Preserve the existing layout, spacing, borders, typography, alignment, icons, and Fact Card dimensions exactly as they are.
* Do not modify the position of any badge or affect surrounding content.
* Ensure the new rectangular rounded badges render consistently in both the on-screen preview and the exported PDF, fully compliant with Amazon KDP print standards.
* When you do this, the content position and alignment of each box should remain the same.

1.) Reduce the **Page References** font size on all **Index pages** to a professional, KDP-friendly size while maintaining excellent readability and a clean layout.

Implement the following logic:

* Reduce the font size of **Page References** (e.g., `6, 9, 14, 17, 25...`) so they are slightly smaller than the **Index Entry (Topic)** text.
* Keep **Index Entry (Topic)** names (e.g., **Animals**, **Birds**, **Mathematics**) in **bold**.
* Keep **Page References** in **regular (normal)** font weight.
* Maintain a clear visual hierarchy where the topic name stands out and the page numbers remain easy to read.
* Automatically adjust the font size based on the available column width to prevent overflow or excessive line wrapping.
* Preserve consistent spacing between the topic name, leader dots, and page references.
* Ensure wrapped page references align correctly beneath the first line without affecting the topic text.
* Apply the same font size and formatting consistently across every Index page.
* Ensure the final typography complies with Amazon KDP print standards, providing a clean, balanced, and professional appearance with no overlapping or clipped text.

2.) Required Enhancement: "MAP OF YOUR ADVENTURE" Marker Selection Logic

Currently, the **"MAP OF YOUR ADVENTURE"** page supports only one display mode, where the SVG transparent icons uploaded by the user through the **"MAP OF YOUR ADVENTURE Bulk SVG Upload (Index)"** section are mapped to each index item.

Enhance this feature by adding a second display mode, allowing the user to choose between **SVG Icons** and **Bullet Points**.

### Add a New Marker Style Option

Create a new setting named:

**Marker Style**

Provide the following two options:

* **SVG Icons**
* **Bullet Points**

### Logic

* If the user selects **SVG Icons**, map the uploaded transparent SVG icons from the **"MAP OF YOUR ADVENTURE Bulk SVG Upload (Index)"** section to the corresponding items on the **"MAP OF YOUR ADVENTURE"** page using the existing icon-mapping logic.
* If the user selects **Bullet Points**, ignore the uploaded SVG icons completely and display clean, professional bullet points for every index item instead.
* Only one marker style may be active at a time.
* Switching between **SVG Icons** and **Bullet Points** should update the preview immediately without requiring the user to regenerate the project.
* The selected marker style must also be applied consistently in the PDF preview, final PDF, and all future export formats.

### Bullet Point Requirements

When **Bullet Points** is selected:

* Use a clean, professional bullet style.
* Apply the same bullet style to every index item.
* Maintain consistent size, alignment, indentation, spacing, and vertical centering.
* Ensure the bullets match the overall typography and layout of the page.

### SVG Icon Requirements

When **SVG Icons** is selected:

* Continue using the uploaded transparent SVG icons.
* Preserve the existing icon-to-index mapping.
* Maintain consistent icon size, spacing, alignment, and vector quality.
* Render only the uploaded SVG icons.

### Expected Behavior

**Marker Style = SVG Icons**

* Display uploaded SVG icons beside each index item.
* Hide bullet points.

**Marker Style = Bullet Points**

* Display bullet points beside each index item.
* Hide all SVG icons.

At any time, only the user-selected marker style should appear on the **"MAP OF YOUR ADVENTURE"** page, ensuring a clean, consistent, and predictable layout.

